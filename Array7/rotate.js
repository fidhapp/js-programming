//? rotate the matrix 90 degree in the c;ock wise direction

function rotate(mat){
    let n=mat.length;
    let res= new Array(n).fill(0).map((ele)=>new Array(n).fill(0));
    for(let k=0;k<2;k++){
        
    }
}